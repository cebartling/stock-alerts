name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Run SwiftLint
        run: swiftlint --strict

  test:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Cache DerivedData
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('project.yml', 'StockAlerts/**', 'StockAlertsTests/**') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-

      - name: Import signing assets
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          BUILD_PROVISIONING_PROFILE_BASE64: ${{ secrets.BUILD_PROVISIONING_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          CERT_PATH="$RUNNER_TEMP/build_certificate.p12"
          PROFILE_PATH="$RUNNER_TEMP/build_pp.provisionprofile"
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"

          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o "$CERT_PATH"
          echo -n "$BUILD_PROVISIONING_PROFILE_BASE64" | base64 --decode -o "$PROFILE_PATH"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH" login.keychain

          PROFILE_PLIST=$(security cms -D -i "$PROFILE_PATH")
          PROFILE_UUID=$(echo "$PROFILE_PLIST" | plutil -extract UUID raw -o - -)
          PROFILE_NAME=$(echo "$PROFILE_PLIST" | plutil -extract Name raw -o - -)

          # Xcode 16 reads macOS provisioning profiles from UserData/Provisioning Profiles.
          # Also drop a copy in the legacy MobileDevice path for older toolchains.
          XCODE_PROFILE_DIR="$HOME/Library/Developer/Xcode/UserData/Provisioning Profiles"
          LEGACY_PROFILE_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$XCODE_PROFILE_DIR" "$LEGACY_PROFILE_DIR"
          cp "$PROFILE_PATH" "$XCODE_PROFILE_DIR/${PROFILE_UUID}.provisionprofile"
          cp "$PROFILE_PATH" "$LEGACY_PROFILE_DIR/${PROFILE_UUID}.provisionprofile"

          echo "Imported provisioning profile: $PROFILE_NAME ($PROFILE_UUID)"
          echo "PROVISIONING_PROFILE_NAME=$PROFILE_NAME" >> "$GITHUB_ENV"

      - name: Diagnose signing setup
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: |
          PROFILE_PATH="$RUNNER_TEMP/build_pp.provisionprofile"
          echo "=== Profile metadata ==="
          # GitHub masks any value matching a secret. If the profile's team matches
          # DEVELOPMENT_TEAM, both render as ***. If they differ, the mismatch is
          # visible because only one is masked.
          security cms -D -i "$PROFILE_PATH" | plutil -extract Name raw -o - -
          security cms -D -i "$PROFILE_PATH" | plutil -extract UUID raw -o - -
          echo "Profile TeamIdentifier:"
          security cms -D -i "$PROFILE_PATH" | plutil -extract TeamIdentifier.0 raw -o - -
          echo "Profile entitlements:"
          security cms -D -i "$PROFILE_PATH" | plutil -extract Entitlements xml1 -o - - || true
          echo "DEVELOPMENT_TEAM secret length: ${#DEVELOPMENT_TEAM} (expect 10)"

          echo ""
          echo "=== Profile directories ==="
          ls -la "$HOME/Library/Developer/Xcode/UserData/Provisioning Profiles/" 2>/dev/null || echo "(missing)"
          ls -la "$HOME/Library/MobileDevice/Provisioning Profiles/" 2>/dev/null || echo "(missing)"

          echo ""
          echo "=== Codesigning identities ==="
          security find-identity -v -p codesigning

      - name: Write Local.xcconfig
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: printf 'DEVELOPMENT_TEAM = %s\n' "$DEVELOPMENT_TEAM" > Local.xcconfig

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Run tests
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
        run: |
          ./scripts/test.sh \
            -resultBundlePath build/StockAlerts.xcresult \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Development" \
            DEVELOPMENT_TEAM="$DEVELOPMENT_TEAM" \
            PROVISIONING_PROFILE_SPECIFIER="$PROVISIONING_PROFILE_NAME"

      - name: Upload xcresult
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: StockAlerts.xcresult
          path: build/StockAlerts.xcresult

      - name: Cleanup keychain
        if: always()
        run: security delete-keychain "$RUNNER_TEMP/app-signing.keychain-db" || true
