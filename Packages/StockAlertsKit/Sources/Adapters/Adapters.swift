// Adapters module — infrastructure that conforms to the Domain ports.
// Depends on Application + Domain; this is the only package module allowed
// to import SwiftData, UserNotifications, Security, AppKit, and URLSession.
//
// Phase 0 placeholder. Phases 2 and 5 move the SwiftData repositories,
// Finnhub service, notification scheduler, keychain, and power observer here.
