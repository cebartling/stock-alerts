# Self-review decisions log

This file captures self-review findings that were *rejected* (kept as-is)
so future review passes don't re-flag them with the same rationale. It
does not record resolved findings — those live in git history.

## 2026-04-27 main (branch)

- **StockAlertsTests/QuoteEngineTests.swift:24** — `makeQuote` defaulting OHLC to `price` masks realistic OHLC invariants
  - **Severity:** low
  - **Reason for rejection:** YAGNI — no test reads open/high/low today; alert evaluation only reads price/changePercent. If a future test needs realistic OHLC, that test can pass them explicitly.
  - **Reviewed by:** opus
- **StockAlerts/Services/FinnhubQuoteService.swift:30** — Non-optional Decodable fields will fail decode if Finnhub returns null
  - **Severity:** low
  - **Reason for rejection:** Consistent with existing c/pc/t decoding — the same null-throws-decoding exposure exists today; equity symbols only is the implicit scope. Revisit if non-equity assets are added.
  - **Reviewed by:** opus
